package deck.game;

import deck.Card; 

// static method(最前最簡單) -> instance method
public class HandManager {
  private Card[] cards;

  //constructor
  public HandManager(Card[] cards) { 
    this.cards = cards;
  }

 // state method >> instance maes
  public boolean isFullHouse() {  //無In put 唔影響 一個class value
    // calculate this.cards
    // logincla cosing    //留意依個
    return false;
  }
}

//use for relaionship
// 轉抉